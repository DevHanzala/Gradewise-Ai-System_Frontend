Options -MultiViews
RewriteEngine On
RewriteBase /

# Allow assets and real files to be served normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirect EVERYTHING else to index.html
RewriteRule ^.*$ /index.html [L]
